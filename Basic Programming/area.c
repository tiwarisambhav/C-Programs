#include<stdio.h>
int main()
{
               float area, a,b;
               printf("value of length and braeth");
               scanf("%f%f",&a,&b);
               area=a*b;
               printf("area=%f",area);
               return 0;
}